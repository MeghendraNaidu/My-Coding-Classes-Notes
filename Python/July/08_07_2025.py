
# Set => Unordered, finite, unique elements
# Set itself is "Mutable" But it takes the Immutable Elements
# set1 = {1, 4, 5, 2, "str1", "str2"}
# print(set1)
# print(len(set1))

list1 = [1, 1, 22, 22, 'str1', 'str1', 55]
# print(set(list1))
# print(list(set(list1)))

# Frozenset => Forzenset itself is Immutable and inside elements also immutable remaining same as to set.
# frznset1 = frozenset([1, 2, 3, 'str1', 'str2']) # For forzenset we need to use the square brackets for converting into single element
# print(frznset1)

# Boolean =>True or Flase
# print(2 > 3)
# print(3 > 2)
# print(-5 > 0)
# print(-5 >= -5)

# None => 
# a = None
# print(a)
# print(type(a))


# Variables
# Naming Convensions
# Rules
#     a-z or A-Z or _
#     Variables Name should not start with numbers.
#     Except UnderScore other special characters should not use in Variables
#     Python reserved Keywords should not use in Variables.
#     Try to not use the inbuilt functiona Names in Variables.
#     Do not use Space in Between Variables.

# 3 Type of cases => snake case, camel case, pascal case.
#     userinputconformpassword
#     Pascal Case => UserInputConformPassword # For Pascal Case every first letter of every word should be "Capital"
#     Camel Case => userInputConformPassword # For Camel Case Only first letter should be "Small" remaining words of first letter should be "Capital"
#     Snake Case =>user_input_conform_password # For Snake Case after every word "UnderScore" should be use.
#     In Python  we mostly use the Snake Case and Pascal Case.

# Name the variables properly. Name should convey some meaning
